/**
 * this program will take strings in through stdin until stdin is closed.
 * it will then echo back all the string that were entered in a topographically sorted order.
 */
int main(int argc, char *argv[]) {
}
